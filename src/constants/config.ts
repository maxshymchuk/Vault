const config = {};

export enum Status {
    Idle = 'idle',
    Loading = 'loading',
    Success = 'success',
    Fail = 'fail'
}

export const Events = {
    Notification: 'notification',
};

export default config;